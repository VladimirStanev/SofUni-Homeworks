using System;

namespace _01._Advertisement_Message
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Message message = new Message();
            message.Phrases =  new string[] { "Excellent product.", "Such a great product.", "I always use that product.", "Best product of its category."
                                             , "Exceptional product.", "I can't live without this product." };
            message.Events = new string[] { "Now I feel good.", "I have succeeded with this product.", "Makes miracles. I am happy of the results!"
                                           , "I cannot believe but now I feel awesome.", "Try it yourself, I am very satisfied.", "I feel great!" };
            message.Authors = new string[] { "Diana", "Petya", "Stella", "Elena", "Katya", "Iva", "Annie", "Eva" };

            message.Cities = new string[] { "Burgas", "Sofia", "Plovdiv", "Varna", "Ruse" };

            int numOfMessages = int.Parse(Console.ReadLine());

            for(int i = 0; i < numOfMessages; i++)
            {
                Random random = new Random();
                int randomPhrasePosition = random.Next(message.Phrases.Length);
                string newPhrase = message.Phrases[randomPhrasePosition];

                int randomEventPosition = random.Next(message.Events.Length);
                string newEvent = message.Events[randomEventPosition];

                int randomAuthorPosition = random.Next(message.Authors.Length);
                string newAuthor = message.Authors[randomAuthorPosition];

                int randomCityPosition = random.Next(message.Cities.Length);
                string newCity = message.Cities[randomCityPosition];

                Console.WriteLine($"{newPhrase} {newEvent} {newAuthor} – {newCity}.");
            }
        }      
    }
    public class Message
    {
       
        public string[] Phrases { get ; set; }

        public string[] Events { get; set; }

        public string[] Authors { get; set; }

        public string[] Cities { get; set; }
    }
}
